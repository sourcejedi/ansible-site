- hosts: drystone
  roles:
    - role: repos-apt
      repos_apt: debian-stretch

    - sourcejedi.etckeeper

    - { role: sourcejedi.systemd-journal,
                         systemd_journal__remove_syslog_daemon: no } # keep the same for now
    - users

    # Note - remote administration is not permitted.
    # We run ssh server but use AllowGroups.
    - openssh-server

    - packages-gui
    - packages-trash-cli
