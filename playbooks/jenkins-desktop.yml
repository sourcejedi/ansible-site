- hosts: drystone
  roles:
    - role: repos-apt
      repos_apt: debian-stretch

    - sourcejedi.etckeeper

    - users
    - sourcejedi.systemd-journal
    # Note - remote administration is not permitted on jenkins-destop.
    - openssh-server

    - remove-mlocate-on-deb
    - { role: bup-daily,
              bup_daily__cron_prefix: '50-' }

    - packages-gui
    - packages-trash-cli
