# jenkins-desktop /home directory

This directory contains both user home directories, and shared files.

The shared files are available in the sub-directory "shared".

---

Recovery information:

The real storage for shared files is in two hidden directories:
"/home/.shared-internal" and "/home/.shared-internal-separate".
This includes shared photo collections.
