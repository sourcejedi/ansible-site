# jenkins-desktop file system

This file system contains:

/home - User files (including shared files).

/data - Mount point for backup/secondary hard drive.

(Everything else - Operating system, application software etc.)
